// Usage: import pinia from './store';
import { createPinia } from 'pinia';

// Create a Pinia instance to use in our store
const pinia = createPinia();

// Export the Pinia instance to use in main
export default pinia;
